import React from 'react';

function About (){
    return (
        <>
        <h1>REACT ROUTER</h1>
        <p>React Router is a standard library for routing in React. It enables the navigation among views of various components in a React Application, allows changing the browser URL, 
           and keeps the UI in sync with the URL.</p>
        </>
    )
}

export default About